def solution():
    
    pads_per_week = 2
    sheets_per_pad = 30
    weeks_per_month = 4
    total_sheets = pads_per_week * weeks_per_month * sheets_per_pad
    result = total_sheets
    return result

print(solution())