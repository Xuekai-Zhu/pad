def solution():
    
    total_people = 90
    group_size = 9
    fraction_of_groups = 3/5
    seashells_per_group = 2
    total_groups = group_size * total_groups
    total_seashells = (3/5) * total_groups * seashells_per_group
    result = total_seashells
    return result

print(solution())