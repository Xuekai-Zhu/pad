def solution():
    
    cost_to_transport = 2
    cost_per_bag = 20
    total_cost = cost_to_transport + cost_to_transport
    profit = 400
    selling_price_per_bag = 30
    num_bags = (profit - total_cost) / selling_price_per_bag
    result = num_bags
    return result

print(solution())