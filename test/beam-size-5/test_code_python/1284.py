def solution():
    
    minutes_per_day = 15
    days_per_week = 5
    weeks = 4
    total_minutes = minutes_per_day * days_per_week * weeks
    result = total_minutes
    return result

print(solution())