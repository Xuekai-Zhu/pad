def solution():
    
    weekly_allowance = 4
    total_allowance = 60
    weeks = 0
    while total_allowance < total_allowance:
        weeks += 8
        total_allowance += weekly_allowance
    result = weeks
    return result

print(solution())