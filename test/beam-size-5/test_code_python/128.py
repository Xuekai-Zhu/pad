def solution():
    
    pam_rides = 2
    fred_rides = 4
    luge_rides = 2
    tickets_per_rider = 6
    total_tickets = (pam_rides + fred_rides) * tickets_per_rider
    result = total_tickets
    return result

print(solution())