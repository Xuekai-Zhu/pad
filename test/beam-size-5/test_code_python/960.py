def solution():
    
    episode_length = 20
    total_episodes = episode_length / 2
    total_minutes = total_episodes * episode_length
    result = total_minutes
    return result

print(solution())