def solution():
    
    notebook_price = 1.5
    ballpen_price = 0.5
    num_notebooks = 5
    num_ballpen = 1
    total_cost = (notebook_price * num_notebooks) + (ballpen_price * num_ballpen)
    result = total_cost
    return result

print(solution())