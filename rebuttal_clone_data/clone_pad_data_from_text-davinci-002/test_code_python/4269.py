def solution():
    land_cost = 20 * 30
    house_cost = 120000
    cow_cost = 1000 * 20
    chicken_cost = 5 * 100
    hours_to_install = 6
    installation_cost = 100 * hours_to_install
    equipment_cost = 6000
    total_cost = land_cost + house_cost + cow_cost + chicken_cost + installation_cost + equipment_cost
    result = total_cost
    return result

print(solution())