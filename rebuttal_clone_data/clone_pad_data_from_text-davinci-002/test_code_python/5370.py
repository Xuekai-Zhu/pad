def solution():
    radius = 14
    pencil_length = 6
    circumference = 2 * math.pi * radius
    total_length = circumference / pencil_length
    result = total_length
    return result

print(solution())