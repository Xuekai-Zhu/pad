def solution():
    candles_needed = 3 * Kerry's age
    candles_per_box = 12
    boxes_needed = candles_needed / candles_per_box
    cost_of_boxes = 2.5
    total_cost = cost_of_boxes * boxes_needed
    age = total_cost / 5
    result = age
    return result

print(solution())