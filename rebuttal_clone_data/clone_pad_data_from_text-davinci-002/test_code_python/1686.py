def solution():
    work_hours_per_day = 10
    work_days_per_week = 5
    work_weeks_per_month = 4
    total_work_hours = work_hours_per_day * work_days_per_week * work_weeks_per_month
    result = total_work_hours
    
    return result

print(solution())