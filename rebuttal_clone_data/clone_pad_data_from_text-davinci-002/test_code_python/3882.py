def solution():
    initial_length = 12
    additional_length = initial_length * (3 / 4)
    total_length = initial_length + additional_length
    result = total_length
    return result

print(solution())