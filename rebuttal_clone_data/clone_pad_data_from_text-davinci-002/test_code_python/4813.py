def solution():
    windows = 3
    sheer_cost = 40
    drape_cost = 60
    total_cost = (windows * 2 * sheer_cost) + (windows * 2 * drape_cost)
    result = total_cost
    return result

print(solution())