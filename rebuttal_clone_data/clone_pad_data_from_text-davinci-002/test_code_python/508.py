def solution():
    initial_distance = 30 * 0.5
    second_distance = 2 * initial_distance
    total_distance = initial_distance + second_distance
    result = total_distance
    return result

print(solution())