def solution():
    original_number_of_trees = 7
    number_of_trees_planted = original_number_of_trees * 2
    leaves_per_tree = 100
    total_leaves = number_of_trees_planted * leaves_per_tree
    result = total_leaves
    return result

print(solution())