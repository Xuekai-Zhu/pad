def solution():
    width = 8
    length = 8
    extension = 2
    new_width = width + (extension * 2)
    new_length = length + (extension * 2)
    new_area = new_width * new_length
    result = new_area
    
    return result

print(solution())