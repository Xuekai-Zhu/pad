def solution():
    street_length = 100
    area_ratio = 3
    trees_per_square_meter = 4
    street_area = street_length ** 2
    forest_area = area_ratio * street_area
    total_trees = Forest_area * trees_per_square_meter
    result = total_trees
    return result

print(solution())