def solution():
    flagpole_length = 60
    height_at_half mast = flagpole_length / 2
    total_distance_traveled = (flagpole_length * 2) + height_at_halfmast
    result = total_distance_traveled
    return result

print(solution())