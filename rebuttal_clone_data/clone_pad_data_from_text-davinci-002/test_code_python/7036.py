def solution():
    dresses_melissa = 16 / 2
    dresses_debora = dresses_melissa + 12
    dresses_emily = 16
    total_dresses = dresses_debora + dresses_emily + dresses_melissa
    result = total_dresses
    
    return result

print(solution())