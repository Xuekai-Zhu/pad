def solution():
    swim_speed = 2
    run_speed = 4 * swim_speed
    total_time = 2 + (2 / 2)
    total_miles = (swim_speed * 2) + (run_speed * (2 / 2))
    result = total_miles
    return result

print(solution())