def solution():
    journal_sessions_per_week = 3
    pages_per_session = 4
    weeks = 6
    total_pages = journal_sessions_per_week * pages_per_session * weeks
    result = total_pages
    return result

print(solution())