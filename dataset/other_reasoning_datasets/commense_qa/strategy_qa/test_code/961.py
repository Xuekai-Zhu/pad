def solution():
    charlie_age = "child"
    can_children_be_hotel_managers = False
    if charlie_age != "adult" and not can_children_be_hotel_managers:
        result = "no"
    else:
        result = "yes"
    return result

print(solution())