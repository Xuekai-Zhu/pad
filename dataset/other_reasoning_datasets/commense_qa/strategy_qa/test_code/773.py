def solution():
    pi_info = "infinite"
    human_memory_capacity = "finite"
    if pi_info == "infinite" and human_memory_capacity == "finite":
        result = "no"
    else:
        result = "yes"
    return result

print(solution())