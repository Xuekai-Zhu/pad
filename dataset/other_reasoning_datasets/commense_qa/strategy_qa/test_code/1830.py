def solution():
    rock_density = [insert value]
    air_density = [insert value]
    if rock_density > air_density:
        result = "no"
    else:
        result = "yes"
    return result

print(solution())