def solution():
    # Check if it is safe to eat kidney
    is_kidney_safe_to_eat = True
    if is_kidney_safe_to_eat:
        result = "yes"
    else:
        result = "no"
    return result

print(solution())