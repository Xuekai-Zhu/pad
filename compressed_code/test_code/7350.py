def solution():
    
    grass_field_distance = 10
    forest_distance = 12
    distance_per_day = grass_field_distance + forest_distance
    total_distance = distance_per_day * 5
    result = total_distance
    return result

print(solution())