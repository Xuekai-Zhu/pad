def solution():
    
    num_birds = 2
    round_trips = 10
    distance_per_round_trip = 400 
    total_distance = num_birds * round_trips * distance_per_round_trip
    result = total_distance
    return result

print(solution())