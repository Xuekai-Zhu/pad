def solution():
    
    journal_sessions_per_week = 3
    pages_per_session = 4
    total_weeks = 6
    total_pages = journal_sessions_per_week * pages_per_session * total_weeks
    result = total_pages
    return result

print(solution())