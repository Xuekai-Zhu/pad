def solution():
    
    rows = 4
    bars_per_row = 20
    value_per_bar = 20000
    total_bars = rows * bars_per_row
    total_worth = total_bars * value_per_bar
    result = total_worth
    return result

print(solution())