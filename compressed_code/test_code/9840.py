def solution():
    
    amount_per_bank = 300
    denomination = 20
    bills_per_bank = amount_per_bank / denomination
    total_bills = bills_per_bank * 2
    result = total_bills
    return result

print(solution())