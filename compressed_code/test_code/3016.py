def solution():
    
    current_length = 12
    additional_length = 0.75 * current_length
    total_length = current_length + additional_length
    result = total_length
    return result

print(solution())