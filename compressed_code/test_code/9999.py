def solution():
    
    radius = 14
    diameter = radius * 2
    pencil_length = 0.5 
    pencils_across = diameter / pencil_length
    result = int(pencils_across)
    return result

print(solution())