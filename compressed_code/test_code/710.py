def solution():
    
    movie_length = 1.5 * 60 
    total_movie_length = 2 * movie_length
    miles_per_minute = 1 / 12 
    total_miles = miles_per_minute * total_movie_length
    result = total_miles
    return result

print(solution())