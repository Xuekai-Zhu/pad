def solution():
    
    work_distance = 6 * 2 * 5
    dog_distance = 2 * 2 * 7
    friend_distance = 1
    store_distance = 3 * 2
    total_distance = work_distance + dog_distance + friend_distance + store_distance
    result = total_distance
    return result

print(solution())