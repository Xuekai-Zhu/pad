def solution():
    
    initial_thread = 12
    additional_thread = initial_thread * (3/4)
    total_length = initial_thread + additional_thread
    result = total_length
    return result

print(solution())