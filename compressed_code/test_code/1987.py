def solution():
    
    reel_length = 100
    num_reels = 3
    section_length = 10
    total_sections = (reel_length * num_reels) / section_length
    result = total_sections
    return result

print(solution())