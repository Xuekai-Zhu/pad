def solution():
    
    cans_per_row = 12
    rows_per_shelf = 4
    shelves_per_closet = 10
    total_cans_per_closet = cans_per_row * rows_per_shelf * shelves_per_closet
    result = total_cans_per_closet
    return result

print(solution())