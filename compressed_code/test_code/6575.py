def solution():
    
    first_hour_distance = 2
    second_hour_distance = first_hour_distance * 2
    total_distance = first_hour_distance + second_hour_distance
    result = total_distance
    return result

print(solution())