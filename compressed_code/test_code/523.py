def solution():
    
    blue_tiles = 48
    red_tiles = 32
    total_tiles = 100
    remaining_tiles = total_tiles - (blue_tiles + red_tiles)
    result = remaining_tiles
    return result

print(solution())