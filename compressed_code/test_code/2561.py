def solution():
    
    starting_cows = 200
    first_year_cows = starting_cows + (starting_cows / 2)
    final_cows = first_year_cows + (first_year_cows / 2)
    result = final_cows
    return result

print(solution())