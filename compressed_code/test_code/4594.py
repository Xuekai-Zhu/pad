def solution():
    
    apple_fraction = 1/5
    cherry_fraction = 3/4
    total_fraction = apple_fraction + cherry_fraction
    percentage = total_fraction * 100
    result = percentage
    return result

print(solution())