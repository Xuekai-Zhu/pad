def solution():
    
    box1_price = 1.25
    box2_price = 1.75
    cards_per_box = 6
    total_cost = (box1_price * cards_per_box) + (box2_price * cards_per_box)
    result = total_cost
    return result

print(solution())