def solution():
    
    baths_per_week = 2
    showers_per_week = 1
    total_cleaning_times = (baths_per_week + showers_per_week) * 52
    result = total_cleaning_times
    return result

print(solution())