def solution():
    
    swing_speed = 10
    run_speed = 15
    swing_time = 10
    run_time = 5
    total_distance = (swing_speed * swing_time) + (run_speed * run_time)
    result = total_distance
    return result

print(solution())